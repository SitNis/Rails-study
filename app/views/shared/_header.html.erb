<% if logged_in? %> 
  <p> Welcome, <%= current_user.email %> Guru. </p>
  <p> <%= link_to "Logout",     
          logout_path, 
          method: "delete",
          data: { confirm: 'Are you sure?'}  %></p>
<% else %>
  <p>Welcome!</p>
  <p><%= link_to "Registration", signup_path %></p>
<% end %>